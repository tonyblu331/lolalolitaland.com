---
import Marquee from "@/components/Marquee.astro"

import Ticket from "public/icons/ticket.svg"
import Clock from "public/icons/clock.svg"
---

<section class="relative w-full overflow-hidden" id="tickets">
  <div class="bg-primary-light mx-auto my-[3%] -ml-2 w-[110%] -rotate-3 text-white">
    <Marquee
      content={[
        "Entradas",
        "Entradas",
        "Entradas",
        "Entradas",
        "Entradas",
        "Entradas",
        "Entradas",
        "Entradas",
      ]}
    />
  </div>

  <div class="container mx-auto px-4 py-12">
    <div class="mb-12 text-center">
      <p class="mx-auto max-w-2xl text-lg text-balance text-white">
        Elige el día que mejor se adapte a ti para vivir la experiencia Lolalolitaland.
      </p>
      <div class="bg-primary-light mt-6 inline-block rounded-lg px-4 py-2">
        <p class="flex items-center gap-2 font-medium text-white">
          <Clock class="h-5 w-5" />
          Próximamente disponibles
        </p>
      </div>
    </div>

    <div class="mx-auto grid max-w-4xl grid-cols-1 gap-4 md:grid-cols-2">
      <!-- Día 1: 14 de junio -->
      <div
        class="overflow-hidden rounded-[calc(var(--radius-lg)+1px)] rounded-t-[calc(2rem+1px)] border border-gray-100 bg-white opacity-75 shadow-md transition-all hover:shadow-lg md:rounded-t-[calc(var(--radius-lg)+1px)] md:rounded-tl-[calc(2rem+1px)] md:rounded-bl-[calc(2rem+1px)]"
      >
        <div class="p-6">
          <div class="flex items-start justify-between">
            <div>
              <p class="text-primary text-xs font-semibold tracking-wide uppercase">Día 1</p>
              <p class="mt-1 text-gray-600">Sábado</p>
              <h3 class="mt-1 text-xl font-bold">14 de junio</h3>
            </div>
          </div>
          <div class="mt-6">
            <button
              disabled
              class="flex w-full cursor-not-allowed items-center justify-center gap-3 rounded-lg bg-gray-300 px-4 py-3 font-medium text-gray-600"
            >
              <Ticket title="ticket" class="h-5 w-5 -rotate-18 fill-gray-700" />
              Comprar entrada
            </button>
          </div>
        </div>
      </div>

      <!-- Día 2: 15 de junio -->
      <div
        class="overflow-hidden rounded-[calc(var(--radius-lg)+1px)] rounded-b-[calc(2rem+1px)] border border-gray-100 bg-white opacity-75 shadow-md transition-all hover:shadow-lg md:rounded-tr-[calc(2rem+1px)] md:rounded-b-[calc(var(--radius-lg)+1px)] md:rounded-br-[calc(2rem+1px)]"
      >
        <div class="p-6">
          <div class="flex items-start justify-between">
            <div>
              <p class="text-primary text-xs font-semibold tracking-wide uppercase">Día 2</p>
              <p class="mt-1 text-gray-600">Domingo</p>
              <h3 class="mt-1 text-xl font-bold">15 de junio</h3>
            </div>
          </div>
          <div class="mt-6">
            <button
              disabled
              class="flex w-full cursor-not-allowed items-center justify-center gap-3 rounded-lg bg-gray-300 px-4 py-3 font-medium text-gray-600"
            >
              <Ticket title="ticket" class="h-5 w-5 -rotate-18 fill-gray-700" />
              Comprar entrada
            </button>
          </div>
        </div>
      </div>
    </div>

    <div class="mt-12 text-center">
      <p class="text-sm text-white">
        Las entradas estarán disponibles próximamente. ¡Mantente atento!
      </p>
    </div>
  </div>
</section>
